query GetMapObject($id: ID!){
    getMapObject(id: $id) {
        ...MapObjectFull
    }
}

fragment MapObjectFull on MapObjectType {
    id
    title
    description
    latitude
    longitude
    category {
        id
    }
    tags {
        id
        title
    }
}